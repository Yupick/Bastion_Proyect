name: CI Cliente Web

on:
  push:
    paths:
      - 'qrypta/cliente_web/**'
      - '.github/workflows/ci-cliente-web.yml'
  pull_request:
    paths:
      - 'qrypta/cliente_web/**'
      - '.github/workflows/ci-cliente-web.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: qrypta/cliente_web

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: qrypta/cliente_web/package-lock.json

      - name: Install
        run: npm ci

      - name: Build
        run: npm run build
